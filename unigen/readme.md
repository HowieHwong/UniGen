```
Program Modules
└── Command
    ├── GEN (generation): Data generation module, output: newly generated dataset
    ├── EVAL (evaluation): Model evaluation module, performs inference on the dataset
    ├── JUDGE (judges): Verifies the correctness of the model's inference results
    └── AUG (augmentation): Data augmentation module
```